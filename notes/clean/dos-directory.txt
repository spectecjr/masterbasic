# The directory: one scan, behind everything.
#
# Prose for clean/ only.  (IX+&04) is RFDH, the scan's mode byte, and it
# is tested in fifteen places -- written RFDH-DCHAN so that each of them
# says which field it is reading.

DOC FDHR
    Walk the directory.  There is only one directory scan in the DOS, and
    this is it: behind DIR, behind every file lookup, behind finding
    somewhere to put a new file, and behind rebuilding the map of which
    sectors are in use.

    WHICH OF THOSE IT IS DOING comes in as a mode byte, stored in the
    channel record at RFDH before the scan starts so that the inner loop
    can test it without reloading:

        bit 0   match the file number
        bit 1   collect names for a sorted listing rather than printing
        bit 2   print a full listing, with a heading
        bit 3   match the name, honouring * and ?
        bit 4   match the name, ignoring the type
        bit 5   read through NRSAD, rebuilding the free-sector map
        bit 6   stop at the first free entry rather than at a match

    While it runs it also totals the sectors used and the files on the
    disc and in the current directory, remembers the first free slot in
    FSLOT, and keeps the highest subdirectory tag in MAXT so that making
    a subdirectory can choose one nothing else has.

    DIRECTORIES LONGER THAN FOUR TRACKS are MasterDOS's own, and the
    arithmetic at &4B52 is what pays for them.  SAMDOS fixed the
    directory at four tracks, which the sector map has permanently
    reserved; anything past that is ordinary disc space and has to be
    marked used by hand every time the directory is read.  DTKS is the
    track count: subtract five, and a borrow means the disc is within
    what SAMDOS allowed and there is nothing to do.  Otherwise the extra
    tracks are turned into map bytes -- ten sectors a track, eight
    sectors a byte, so five halves and then a shift -- and that many
    bytes at the front of SAM are filled in.

DOS &4B31 : nothing may interrupt a scan; the buffer is shared
DOS &4B37 : the mode byte, where the inner loop can reach it
DOS &4B3B : no free slot found yet
DOS &4B3E : and no subdirectory tags seen
DOS &4B41 : wind the head back to track 0, where the directory starts
DOS &4B47 : how many tracks of directory this disc has
DOS &4B4F : a full listing wants its heading first
DOS &4B52 : the directory's length in tracks
DOS &4B55 : five or more means more than SAMDOS ever allowed
DOS &4B57 : four or fewer, and the map already has them reserved
DOS &4B59 : how many tracks beyond the four
DOS &4B5D : ten sectors a track, counted in halves
DOS &4B60 : and eight sectors to the byte
DOS &4B64 : the sector map itself
DOS &4B68 : track 4 sector 1 is not reserved, so leave its bit alone

DOS &45E2 expr RFDH-DCHAN
DOS &4B4B expr RFDH-DCHAN
DOS &4BDB expr RFDH-DCHAN
DOS &4CB4 expr RFDH-DCHAN
DOS &4CCB expr RFDH-DCHAN
DOS &5E82 expr RFDH-DCHAN
DOS &67E8 expr RFDH-DCHAN
DOS &680A expr RFDH-DCHAN
DOS &680E expr RFDH-DCHAN
DOS &68BA expr RFDH-DCHAN
DOS &6AD6 expr RFDH-DCHAN
DOS &6B6E expr RFDH-DCHAN
DOS &6B8D expr RFDH-DCHAN
DOS &6E18 expr RFDH-DCHAN
DOS &702B expr RFDH-DCHAN

DOC NRSAD
    Read a directory sector, and rebuild the free-sector map on the way
    past for nothing.

    A directory entry carries the map of the sectors its file occupies,
    and NSAM -- the DOS's map of every sector in use -- is placed so that
    the offsets line up.  So the map can be rebuilt by ORing each entry's
    bytes into NSAM as they arrive off the disc, and a scan that had to
    read the directory anyway comes back having done it.

    THE ALTERNATE REGISTER SET holds the NSAM pointer while the main set
    holds the sector buffer, so the transfer loop can keep both without
    a spare register.  L' doubles as the count of bytes within an entry:
    entries are 256 bytes and NSAM is aligned to match, so L' wrapping to
    zero is both the next NSAM byte and the start of the next entry.  It
    is loaded with &FF at the top so that the first INC L wraps at once
    and the first entry is treated like any other.

    THE FREE ENTRIES ARE THE DIFFICULTY, since their maps must not be
    counted, and there is no time in the loop to test anything per byte.
    NRS22 hoists the test to once per entry -- the first byte of an entry
    is its type, and zero means erased or unused -- and answers it by
    pointing H' either at NSAM's page or at zero.  A free entry's bytes
    are still ORed and still stored, into &00xx, which is ROM, where the
    write does nothing at all.  The discard costs the same as the keep.

    Nothing here can afford a test it does not have to make, which is
    also why both status reads have their port poked into them rather
    than indexed, and why the byte-ready test is an AND against a mask
    kept in B instead of a BIT.

DOS &463F : so the first INC L wraps to zero and takes the NRS22 path
DOS &4641 : the page NSAM is in, kept for NRS22 to reload
DOS &465C value DISK_STATUS_DRQ
DOS &4660 : a live entry: point H' at the map
DOS &4661 : the first byte of an entry is its type; zero is erased or unused
DOS &4664 : a free entry: point H' at ROM, where the OR will go nowhere
DOS &4668 : is a byte waiting?
DOS &466D : into the sector buffer
DOS &4670 : on through NSAM, and count the byte within the entry
DOS &4671 : wrapped, so this is the first byte of the next entry
DOS &4673 : the file's map, ORed into the disc's
DOS &4679 : the sector is finished
DOS &467B : nothing waiting yet

GROUP Directory scan
CONST DIR_MODE_COLLECT = &02 : collect names for a sorted listing
CONST DIR_MODE_LIST = &04 : print a full listing, with a heading

DOC FDH05_1
    One directory entry.  The scan arrives here with HL on its first
    byte, and every field it wants is an offset from there.

    AN ENTRY IS 256 BYTES, one to a quarter of a sector, and the layout
    is SAMDOS's with MasterDOS's own fields added at the top end where
    the ROM's header did not reach:

        +0        type and flags; bits 0-4 the type, 6 protected,
                  7 hidden.  Zero means the slot is free
        +1        the name, ten characters, space padded
        +11       sectors used, high byte first
        +13       first track, bit 7 for side 2, and first sector
        +15       195 bytes of sector map -- which sectors are this
                  file's, in the same bit order as NSAM
        +210      the disc's name in entry 0; the file's nine-byte
                  header in any other
        +220      the tail of the ROM's 48-byte header, date stamp and all
        +&FA      the tag a subdirectory gives to the files inside it
        +&FE      the tag of the directory this file belongs to, 0 for
                  the root
        +&FF      in entry 0, the directory's length beyond four tracks

DOS &4B8B : the type byte, which is also the "is this slot used" byte
DOS &4B8D : a free slot; what happens next depends on why we are scanning
DOS &4B90 : the mode this scan was started with
DOS &4B93 expr DIR_MODE_COLLECT | DIR_MODE_LIST
DOS &4B95 : neither kind of listing, so none of the totals are wanted
DOS &4B98 : the type, kept in the alternate accumulator while HL moves
DOS &4B9C : up to the far end of the entry, where MasterDOS put its own
DOS &4B9F : the tag of the directory this file is in
DOS &4BA0 expr &000B
DOS &4BA0 : eleven bytes in is the sector count, high byte first
DOS &4BA7 : kept for the listing to print
DOS &4BAF : the sectors used on the disc so far
DOS &4BB6 : and the files

# RPT is where the scan has got to inside the sector, so its high
# byte alone steps a whole 256-byte entry.  Twenty-seven sites.

DOS &4887 expr RPT-DCHAN+1
DOS &488A expr RPT-DCHAN
DOS &4C95 expr RPT-DCHAN+1
DOS &4F8E expr RPT-DCHAN
DOS &4F92 expr RPT-DCHAN+1
DOS &4FB7 expr RPT-DCHAN+1
DOS &4FBA expr RPT-DCHAN
DOS &4FD7 expr RPT-DCHAN+1
DOS &5403 expr RPT-DCHAN+1
DOS &5DD5 expr RPT-DCHAN+1
DOS &5E85 expr RPT-DCHAN+1
DOS &5E9D expr RPT-DCHAN+1
DOS &5EB7 expr RPT-DCHAN+1
DOS &5EC0 expr RPT-DCHAN+1
DOS &64B4 expr RPT-DCHAN
DOS &6CDA expr RPT-DCHAN
DOS &6CDF expr RPT-DCHAN+1
DOS &6FDF expr RPT-DCHAN
DOS &6FE5 expr RPT-DCHAN+1
DOS &7003 expr RPT-DCHAN
DOS &7007 expr RPT-DCHAN+1
DOS &703B expr RPT-DCHAN
DOS &703E expr RPT-DCHAN+1
DOS &718C expr RPT-DCHAN
DOS &718F expr RPT-DCHAN+1
DOS &7632 expr RPT-DCHAN+1
DOS &7635 expr RPT-DCHAN

DOS &4C95 : one entry on, which is one whole page of the sector

GROUP Characters
CONST CH_STAR = &2A : the wildcard that matches any run of characters
CONST CH_QUERY = &3F : the wildcard that matches one character
CONST CH_PERIOD = &2E : between a name and its type
CONST CASE_BLIND = &DF : every bit but the one that tells A from a

GROUP Directory entry
CONST NAME_AND_TYPE = &0B : the type byte and the ten characters after it

DOC CKNAM
    Does this entry's name match what the user asked for?

    Eleven bytes are compared: the type byte and the ten characters of
    the name.  The type is skipped when the mode says only the name
    matters, by entering the loop one instruction in so that the first
    INC DE and INC HL step over it.

    THE COMPARISON IS CASE BLIND FOR NOTHING.  XOR leaves the bits in
    which the two characters differ, and AND &DF throws away bit 5 --
    the one bit that separates A from a.  What is left is zero when the
    characters are the same letter in either case.  No table, no range
    test, no branch.

    IT ALSO MERGES SIX PAIRS THAT ARE NOT LETTERS, because bit 5 is what
    separates those too:

        @ and `     [ and {     \ and |
        ] and }     ^ and ~     _ and DEL

    So DIR "A[" lists A{ as well, and a file saved as X^ can be loaded
    as X~.  Nothing stops those names being used: the ROM's only test on
    a file name is its length, and a null one, so any byte a BASIC string
    can hold can go into a directory entry.  The same arithmetic makes
    every character from space to ? equal to a control code thirty-two
    below it, which needs a program to put the control code there but is
    otherwise no different.

    Twenty-six pairs were wanted and thirty-two came free, which for one
    AND is a fair trade, but it is the kind of thing that is worth
    knowing before wondering why two files answer to one name.

    * MATCHES THE REST, unless a period follows it.  A star with nothing
    after it, or with anything other than a period, is a match there and
    then: "*" and "A*" match everything.  A star followed by a period
    means the type still has to match, which is how a pattern can select
    on type alone.

    ? matches whatever is in that one position and nothing else changes.

    Zero flag set on the way out means it matched.

DOS &4CC6 value NAME_AND_TYPE
DOS &4CC8 : the pattern, as the user typed it
DOS &4CCF : the type is not being matched, so step over it
DOS &4CD1 : the next character of the pattern
DOS &4CD2 value CH_STAR
DOS &4CD6 value CH_QUERY
DOS &4CD8 : one character of anything
DOS &4CDA : the bits in which the two characters differ
DOS &4CDB value CASE_BLIND
DOS &4CDD : they differ in something other than case
DOS &4CE3 : the whole pattern matched
DOS &4CE6 : was the star the last character of the pattern?
DOS &4CE7 : it was, so everything after it matches
DOS &4CEC value CH_PERIOD
DOS &4CEE : not a period, so the star swallows the rest

DOS &4CD4 : a star, so the rest of the pattern decides
